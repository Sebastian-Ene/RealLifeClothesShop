import { Products } from './Products';
import { ProductsComponent } from '../products/products.component';

/*describe('Products', () => {
  it('should create an instance', () => {
    expect(new Products(choseColor, chosePrice)).toBeTruthy();
  });
});*/
